name: CI

on:
  push:
    branches:
      - main
    tags:
      - "*"
  pull_request:
    branches:
      - "*"


jobs:
  tests_run:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
        python-version:
          - '3.11'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install Dependencies
        run: python3-tk tk8.6-blt2.5
      - run: |
          python -m pip install --upgrade pip
      - run: |
          pip install tox
      - run: |
          tox -e py3.11
          tox -e safety
